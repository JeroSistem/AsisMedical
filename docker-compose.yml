version: '3.9'

services:
  web:
    build: .
    container_name: AsisMediCare_web
    env_file: .env.local
    ports:
      - '9002:9002'
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development
    command: npm run dev
